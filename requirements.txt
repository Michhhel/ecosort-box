# --- Partie web (mon périmètre) ------------------------------------------
Django>=5.0,<6.0
gunicorn>=22.0          # serveur WSGI de production dans Docker
whitenoise>=6.6         # sert les fichiers statiques sans nginx
requests>=2.31          # téléchargement des images produits (vue classify)

# --- À décommenter quand les vrais modules des coéquipiers arriveront ----
# tensorflow-cpu>=2.16  # ml_model : version CPU (pas de GPU en déploiement)
# beautifulsoup4>=4.12  # scraper  : selon la solution retenue par le coéquipier
# lxml>=5.0
