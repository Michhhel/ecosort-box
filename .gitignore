# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# --- Django ------------------------------------------------------------------
staticfiles/
db.sqlite3
*.log

# --- Données & modèles (JAMAIS sur GitHub : trop lourds) --------------------
# Dataset Kaggle "Garbage Classification"
dataset/
data/
*.zip

# Poids du modèle : à partager via Git LFS ou une release GitHub,
# pas dans l'historique git classique.
*.h5
*.keras

# Retours utilisateurs collectés en local (images + feedback.jsonl)
var/

# --- Environnement / IDE ------------------------------------------------------
.env
.idea/
.vscode/
.DS_Store


MODIFICATIONS.md
# --- Exception (partie ml) ---------------------------------------------------
# Le modele FINAL est un livrable : le Dockerfile fait "COPY . ." et
# l'enseignant doit pouvoir "cloner + docker build" sans etape manuelle.
# La regle *.h5 ci-dessus continue de bloquer les modeles d'experimentation.
!ml_model/modele_eco_sort.h5
