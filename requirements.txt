# --- Partie web -------------------------------------------------------------
Django>=5.0,<6.0
gunicorn>=22.0          # serveur WSGI de production dans Docker
whitenoise>=6.6         # sert les fichiers statiques sans nginx
requests>=2.31          # téléchargement des images produits (vue classify) + scraping

# --- Modèle de deep learning (ml_model) --------------------------------------
tensorflow-cpu>=2.16    # version CPU (pas de GPU en déploiement)
pillow>=10.0            # décodage des images (classifier.py)

# --- Scraping Jumia (scraper) -------------------------------------------------
beautifulsoup4          # parsing HTML
lxml                    # parseur rapide pour BeautifulSoup

# --- Compréhension / correction orthographique --------------------------------
rapidfuzz               # correction via le vocabulaire métier (motre -> montre)
pyspellchecker          # correcteur orthographique français généraliste

# --- Repli scraping pour le contenu rendu en JavaScript (optionnel) -----------
# Nécessite un navigateur Chrome installé ; absent de l'image Docker, le
# scraper bascule proprement sur [] si ce repli n'est pas utilisable.
selenium
webdriver-manager
